# NodeGraphSystem
 
A program which enables the user to create graphs via node - each node can connect to other nodes of the same type
There are multiple abstract types of nodes which each can only connect to each other.
There's a special type of 'bridge' node which is the only one which can connect and be connected from and to any node type.
For examples and interaction demonstration see "Issues" tab.
